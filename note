./ will show in current floder
../ will show outside the folder

header ,main, footer -->
article, section,aside -->

-- table --
    border to get all border
--github--
push
table attractive
--note to use-- action
-- content > p in css -- read(sign >) 
-- column or row span-- merge more than 1 column or row
--form--
--css footer--
-- css alginment--
--transition--
--transform--

JAVASCRIPT

return javascript type
1. return "<h1>"+txt+"</h1>"
2. return `<h1>${txt}</h1>` that is `````````(above tab)

queryselector 
can selct id, tag, class
it will select only first element first one alone 
it can be used like css

queryselector all
select all class , tag, id 
